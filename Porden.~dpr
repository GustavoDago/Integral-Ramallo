program Porden;

uses
  Forms,
  Orden in 'ORDEN.PAS' {FOrden};

{$R *.RES}

begin
  Application.Title := 'Administrador de Ordenes de Pago';
  Application.CreateForm(TFOrden, FOrden);
  Application.Run;
end.
