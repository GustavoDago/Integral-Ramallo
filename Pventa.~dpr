program Pventa;

uses
  Forms,
  Venta in 'Venta.PAS' {FormVenta};

{$R *.RES}

begin
  Application.Title := 'Módulo de Ventas';
  Application.CreateForm(TFormVenta, FormVenta);
  Application.Run;
end.
